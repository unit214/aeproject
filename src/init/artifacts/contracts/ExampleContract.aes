@compiler >= 6

main contract Example =
  type state = option(int)

  entrypoint init() : state = None

  stateful entrypoint set(x : int) : unit = put(Some(x))
  entrypoint get() : option(int) = state
